// terminalConfig.js

export const config = {
    fontSize: 18,
    fontFamily: 'Courier New, monospace',
    fontWeight: 'normal',
    bgColor: '#000000',
    fgColor: '#FFFFFF',
    cursorVerticalOffset: 0, // used for CRT shake, not layout
    charWidth: 10,        // Fixed cell width in px
    charHeight: 21,       // Fixed cell height in px
    useFixedCellSize: false,                     // <- disable hardcoded cell size
    cursorOffsetX: -1,
    cursorOffsetY: -1.5,

  };

  